# 📄 توثيق متطلبات وقرارات تطوير متجر البيلسان أونلاين (Project Requirements & Decisions Document)

تم إنشاء هذا المستند لتجميع كافة الملاحظات والصوتيات والصور والأفكار التي تم الاتفاق عليها لتسجيلها بوضوح تام، وذلك للعودة إليها وتطبيقها مستقبلاً وقتما تشاء بدون أي تعديل حالي على الكود.

---

## 1️⃣ نوع الطلب، رسوم التوصيل، وصيغة الواتساب الموحدة (Order Options & WhatsApp Integration)

### 🔹 الملاحظة والسبب:
الخيارات السابقة (حجز / توصيل) كانت تسبب إرباكاً للزبائن حول ما إذا كان المطلوب استلاماً من المحل أو توصيلاً للمنزل.

### 🔹 القرار النهائي:
1. **استلام من المتجر (Store Pickup)**:
   - لا يطالب الزبون بإدخال عنوان السكن.
   - تكلفة التوصيل: **0.00 د.أ**.
2. **توصيل إلى المنزل (Delivery)**:
   - يتم المطالبة بإدخال العنوان بالتفصيل.
   - تضاف **2.00 د.أ** كرسوم توصيل ثابتة لجميع المحافظات إلى الإجمالي الكلي للطلب تلقائياً.

### 🔹 صيغة رسالة الواتساب المحددة والمعتمدة للطلب:
```text
🛵 طلب جديد من [اسم الزبون]

• [اسم المنتج] (المقاس [المقاس]، اللون [اللون]) × [الكمية]

الإجمالي الكلي: [المبلغ الكلي شامل 2 د.أ للتوصيل إذا كان توصيل] د.أ

📞 رقم الهاتف: [رقم الهاتف]

📍 العنوان: [العنوان الكامل أو "استلام من المتجر"]

📏 طول الزبونة: [الطول] سم

⚖️ الوزن الحقيقي: [الوزن] كغم

⌚ تم الإرسال من موقع البيلسان أونلاين
```

---

## 2️⃣ مرجعية الألوان ولوحة الألوان للأدمن (Color Guide & Admin Palette Selector)

### 🔹 الملاحظة والسبب:
تختلف درجات ألوان الأقمشة بحسب الإضاءة والكاميرات، وكتابة الألوان نصياً قد تسبب أخطاء إملائية أو عدم دقة باللون المطابق.

### 🔹 القرار النهائي:
1. **مرجعية الألوان للزبائن (Color Guide Modal)**:
   - إنشاء دليل مرجعي يحتوي على عينات صور الأقمشة الحقيقية الملتقطة بوضوح (مثل صورة "اخضر فاتح" المزودة).
   - إدراج زر **🎨 مرجعية الألوان** في صفحة المنتج `ProductDetails` يفتح النافذة للزبون قبل الاختيار.
2. **لوحة ألوان تفاعلية للأدمن (Admin Color Palette Selector)**:
   - في صفحة إضافة وتعديل المنتجات `AdminProducts` وفي مرجعية الألوان، إضافة لوحة ألوان (Color Palette Chips) تتيح اختيار اللون بنقرة زر واحدة.
   - ربط الألوان المحددة بالعينات المصورة في المرجعية تلقائياً.

---

## 3️⃣ صور المنتجات المصغرة في الطلبات واللوحة والفواتير (Order Item Thumbnails)

### 🔹 الملاحظة والسبب:
تتشابه أسماء بعض العباءات والأطقم، مما يصعّب التمييز البصري السريع للطلب في لوحة الأدمن.

### 🔹 القرار النهائي:
1. حفظ رابط صورة المنتج (`image`) في كائن الطلب بالسلة وقاعدة البيانات (`order.items[].image`).
2. عرض صورة مصغرة (Thumbnail) لكل عنصر بجانب الاسم والمقاس واللون في:
   - بطاقات الطلبات في لوحة الأدمن (`AdminOrders` و `AdminDashboard`).
   - معاينة وفاتورة الطباعة (`Printable Invoice Modal`).

---

## 4️⃣ خريطة الملفات والمكونات المنفذة (File Index & Implemented Components)

- `backend/models/ColorGuide.js`: [جديد] نموذج Mongoose لإدارة دليل الألوان (الاسم، كود اللون، رابط الصورة).
- `backend/routes/colorRoutes.js`: [جديد] مسارات API للـ CRUD لدليل الألوان وتوليد الألوان الافتراضية.
- `backend/models/Order.js`: إضافة `image` لعناصر الطلب وحقول `deliveryFee` و `totalPrice`.
- `backend/routes/orderRoutes.js`: احتساب رسوم التوصيل تلقائياً واحتساب المجموع الإجمالي.
- `backend/server.js`: تسجيل مسارات `/api/colors`.
- `frontend/src/pages/AdminColors.jsx`: [جديد] صفحة للأدمن لإدارة دليل الألوان وعينات الأقمشة المصورة.
- `frontend/src/components/ColorGuideModal.jsx`: [جديد] نافذة منبثقة تفاعلية لعرض عينات الأقمشة المصورة للزبائن.
- `frontend/src/pages/AdminProducts.jsx`: إضافة لوحة ألوان بصرية تفاعلية (Color Chips Selector).
- `frontend/src/pages/ProductDetails.jsx`: إضافة زر مرجعية الألوان وعرض عينات الألوان التفاعلية.
- `frontend/src/pages/Delivery.jsx`: احتساب 2.00 د.أ توصيل وتنسيق رسالة الواتساب بالهيكل المعياري.
- `frontend/src/pages/Reservation.jsx`: ضبط خيار "استلام من المتجر" وتنسيق رسالة الواتساب بالهيكل المعياري.
- `frontend/src/pages/AdminOrders.jsx`: عرض الصور المصغرة بجانب كل عنصر وفي الفاتورة الجاهزة للطباعة.
- `frontend/src/AppRoutes.jsx` & `AdminDashboard.jsx`: تسجيل المسار وإضافة رابط لوحة دليل الألوان.

---

## 5️⃣ حالة التنفيذ (Implementation & Deployment Status)

- **الحالة**: ✅ **تم التنفيذ بالكامل والنشر بنجاح (Fully Implemented & Deployed)**.
- **التاريخ**: 2026-09-15.
- **المستودع (Git Commit)**: تم الرفع والتطبيق المباشر على فرع `main -> main`.

---

## 6️⃣ نظام العروض والتخفيضات الانتقائي (Selective Product Offers Campaign)

### 🔹 الملاحظة والسبب:
في النظام السابق، كانت العروض تطبق بشكل عام (Global) على جميع المنتجات التي تحتوي على سعر قبل الخصم (`originalPrice`) أو شارة خصم (`discountTag`). عند إدخال عروض وتخفيضات مستقبلية لعدة منتجات، لم يكن بمقدور الأدمن اختيار مجموعة محددة من المنتجات (مثلاً 10 منتجات من أصل 50) لتفعيل العرض عليها فقط مع العداد التنازلي وإبقاء البقية في وضع الاستعداد.

### 🔹 القرار النهائي والتنفيذ:
1. **تخصيص المنتجات المشمولة بالعرض (Selective Product Selection)**:
   - إضافة خاصية `products` (مصفوفة معرفات المنتجات ObjectIds) إلى نموذج `OfferSetting`.
   - إتاحة واجهة تفاعلية للأدمن في صفحة `AdminOffersConfig` لاختيار المنتجات المشمولة بالعرض التنازلي، مع أدوات بحث سريع وأزرار تحديد ("تحديد الكل"، "إلغاء تحديد الكل"، "تحديد المنتجات ذات الخصم 🏷️").
2. **الربط بالعداد التنازلي**:
   - عندما يكون العداد التنازلي نشطاً (`isEnabled: true` والتاريخ ساري)، يتم تفعيل الخصم وعرض السعر المخفض فقط للمنتجات المحددة صراحة ضمن العرض.
   - المنتجات غير المحددة تظهر بسعرها الاعتيادي دون خصم.
   - يمكن للأدمن إضافة أو إزالة منتجات من العرض في أي وقت (قبل أو أثناء تشغيل الحملة).
3. **التراجع التلقائي عند انتهاء العرض**:
   - عند انتهاء العداد التنازلي أو إيقافه، تعود كافة المنتجات تلقائياً لعرض أسعارها الأصلية في المتجر.

### 🔹 الملفات المعدلة:
- `backend/models/OfferSetting.js`: إضافة حقل `products` (مصفوفة `ObjectId` المرجعية لـ `Product`).
- `backend/routes/offerSettingRoutes.js`: تحديث مسار `PUT /api/offer-settings` لحفظ مصفوفة المنتجات المحددة.
- `backend/routes/productRoutes.js`: تحديث `checkOfferSettings` و `formatProduct` لتطبيق الخصم حصرياً للمنتجات المحددة في العرض النشط، وتحديث استعلام `offersOnly`.
- `frontend/src/pages/AdminOffersConfig.jsx`: بناء واجهة اختيار المنتجات المشمولة مع البحث والتحديد التفاعلي ومعاينة عدد المنتجات المحددة.
- `frontend/src/pages/Offers.jsx` & `Home.jsx`: تحديث استعلام المنتجات لطلب عروض الحملة النشطة (`offersOnly=true`).

---

---

## 8️⃣ محرك أكواد الخصم المتقدم ونطاقات العروض المرنة (Multi-Promo Codes & Flexible Offer Scopes)

### 🔹 القرار النهائي والتنفيذ:
1. **نظام أكواد الخصم المتعددة (`PromoCode` Model & API)**:
   - إنشاء نموذج `PromoCode` مستقل يدعم:
     - **رموز متعددة** لكل كود خصم على حدة.
     - **نوع الخصم**: نسبة مئوية (٪) أو خصم مباشر (مبلغ ثابت د.أ).
     - **المدد والتواريخ (Duration)**: تاريخ بدء وتاريخ انتهاء محدد لكل كود.
     - **نطاق التطبيق (Scope)**: تطبيق عام (Global) على كامل السلة، أو مخصص لأقسام محددة (Categories)، أو منتجات محددة (Products).
     - **التحقق التفاعلي**: مسار `/api/promo-codes/validate` الذي يتحقق تلقائياً من تاريخ الصلاحية ونطاق المنتجات والسلة وتطبيق الخصم فوراً.
2. **مرونة نطاق التخفيض التنازلي في العروض**:
   - تحديث نموذج `OfferSetting` لدعم 3 نطاقات لتطبيق التخفيض التنازلي:
     - 🌐 **تطبيق عام (Global)**
     - 📁 **تحديد أقسام معينة (Categories)**
     - 🎯 **منتجات محددة (Selective Products)**
3. **مركز لوحة تحكم الأدمن للخصومات والأكواد (`AdminOffersConfig.jsx`)**:
   - تبويبات منفصلة لإدارة العروض التنازلية وإدارة أكواد الخصم مع نافذة منبثقة تفاعلية لإضافة وتعديل الأكواد والمدد والنطاقات.

### 🔹 الملفات المضافة والمعدلة:
- `backend/models/PromoCode.js`: [جديد] نموذج Mongoose لإدارة أكواد الخصم.
- `backend/routes/promoCodeRoutes.js`: [جديد] مسارات CRUD وتدقيق سريان الأكواد.
- `backend/models/OfferSetting.js` & `offerSettingRoutes.js`: إضافة `scope` و `categories`.
- `backend/routes/productRoutes.js`: دعم نطاقات العروض المختلفة (عام / أقسام / منتجات).
- `backend/server.js`: تسجيل مسارات `/api/promo-codes`.
- `frontend/src/pages/AdminOffersConfig.jsx`: مركز إعدادات العروض وأكواد الخصم بالتبويبات والنوافذ المنبثقة.
- `frontend/src/pages/Delivery.jsx` & `Reservation.jsx`: الربط بالـ Validator المتقدم وحساب الخصم المباشر أو النسبة المئوية.
- `ADMIN_OFFERS_GUIDE.md`: دليل تفصيلي شامل للأدمن باللغة العربية.

---

## 9️⃣ حالة النشر والتحديث الأخير (Latest Deployment Status)

- **الحالة**: ✅ **تم التنفيذ بالكامل والنشر بنجاح (Fully Implemented & Deployed)**.
- **التاريخ**: 2026-09-17.
- **المستودع (Git Commit)**: تم الرفع والتطبيق المباشر على فرع `main -> main` (`feat: implement multi-promo code engine and flexible offer scopes`).

